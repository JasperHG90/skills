<constraint name="pre-existing issues">
Pre-existing issues (e.g. test failures, type hinting, missing docstrings etc.) should never be skipped. They must be addressed by the agent that notices or runs into them.
</constraint>%   
